build:
  docker:
    web: Dockerfile
run:
  web:  gunicorn --workers=2 --bind=0.0.0.0:$PORT farsali.wsgi:application --reload
  worker1: celery -A farsali.celery worker -l info
  worker2: celery -A farsali.celery beat -l info